import React, { useContext } from 'react'
import { AppContext } from '../../context'

const Header = ({ ...pros }) => {
  return (
    <div className={`${pros}`}>
    </div>
  )
}

export default Header
